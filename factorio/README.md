# Factorio server

A basic dockerfile and compose for factorio base game. Disables Space Age
expansion, and allows whitelisting using `config/server-whitelist.json` file.

Build and run using `docker compose up`.
